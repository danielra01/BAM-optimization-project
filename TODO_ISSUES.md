# TODO Issues to Create

Found 3 TODO items that need GitHub issues:

## 1. TODO: Explain what is happening in the seperable case from a geome...

## TODO Found in Code

**File:** `report/mathematical-background.tex`  
**Line:** 82

### Description
Explain what is happening in the seperable case from a geometric point of view!

### Context
```
Therefore, if there exists such a separating parameter $\bar x$, the optimization term $\sum_{j=1}^N L(p_j,\varphi(a_j;\bar x^{(t)}))$ is going to be arbitrarily small for large $t$, i.e. for large weights and biases $t \bar{w_i}$ and $t \bar{b_i}$.
This implies that the minimum of our optimization problem is not attained at a finite parameter $x$ and therefore the optimization problem is not solvable.
\TODO{Explain what is happening in the seperable case from a geometric point of view!}
\begin{rem}
    The idea, that linear separability can be a problem for logistic regression, was inspired by \cite[Linear Separability and Regularization]{Adams2018}.
```

### Location
This TODO was found at line 82 in `report/mathematical-background.tex`.

---
*This issue was automatically generated from a TODO comment in the code.*


---

## 2. TODO: explain why regularization is a good thing to do!

## TODO Found in Code

**File:** `report/mathematical-background.tex`  
**Line:** 88

### Description
explain why regularization is a good thing to do!

### Context
```
\subsection{Regularization}\;\\
solves some problems \TODO{explain why regularization is a good thing to do!}

\TODO{It is fairly obvious that the optimization problem is $C^\infty$. Where should we put this?}
```

### Location
This TODO was found at line 88 in `report/mathematical-background.tex`.

---
*This issue was automatically generated from a TODO comment in the code.*


---

## 3. TODO: It is fairly obvious that the optimization problem is $C^\in...

## TODO Found in Code

**File:** `report/mathematical-background.tex`  
**Line:** 90

### Description
It is fairly obvious that the optimization problem is $C^\infty$. Where should we put this?

### Context
```
solves some problems \TODO{explain why regularization is a good thing to do!}

\TODO{It is fairly obvious that the optimization problem is $C^\infty$. Where should we put this?}
```

### Location
This TODO was found at line 90 in `report/mathematical-background.tex`.

---
*This issue was automatically generated from a TODO comment in the code.*


---

